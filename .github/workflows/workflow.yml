name: Run dbt Models

on:
  schedule:
    - cron: '0 0 * * *' # Schedule to run every day at midnight UTC

jobs:
  run-dbt:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Run dbt
      run: dbt run -d C:/Users/skrishna/Desktop/DBT/dbt-project/dfz
